<%= form_with(model: checklist_item, local: true) do |form| %>
  <% if checklist_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(checklist_item.errors.count, "error") %> prohibited this checklist_item from being saved:</h2>

      <ul>
      <% checklist_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :notes %>
    <%= form.text_area :notes %>
  </div>

  <div class="field">
    <%= form.label :checklist_id %>
    <%= form.text_field :checklist_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
